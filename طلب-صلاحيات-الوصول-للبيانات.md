سوال
# طلب منح صلاحيات الوصول إلى بيانات الأداء
## (Data Access Authorization Request)

---

## 1. الجهة الطالبة
- **المشروع:** لوحة متابعة أداء الموظفين (Agent Performance Dashboard)
- **الغرض من الطلب:** ربط لوحة الأداء بمصدر البيانات الأساسي بشكل مباشر لإلغاء الرفع اليدوي.

---

## 2. الوضع الحالي (المشكلة)

حالياً تتم عملية تحديث البيانات بالطريقة التالية:

```
الشيت الأساسي (مصدر البيانات)
        ↓
استخراج JSON يدوياً من إضافة المتصفح
        ↓
فتح لوحة الإدارة (Admin Portal)
        ↓
لصق الـ JSON يدوياً
        ↓
حفظ البيانات وتحديث اللوحة
```

### المشاكل الناتجة عن هذه الطريقة:
| المشكلة | التأثير |
|---|---|
| الرفع اليدوي | احتمالية حدوث أخطاء بشرية أثناء النسخ واللصق |
| التأخير الزمني | البيانات لا تتحدث لحظياً، وتحتاج تدخل يدوي في كل مرة |
| بيانات غير مكتملة | قد يتم فقدان بعض الصفوف أثناء النقل اليدوي |
| ضغط على الشخص المسؤول | الاعتماد الكامل على شخص واحد لتنفيذ التحديثات |
| صعوبة دعم جميع الأشهر | تخزين البيانات التاريخية بشكل يدوي غير عملي على المدى الطويل |

---

## 3. الحل المقترح

منح السكربت صلاحية **قراءة فقط** على الشيت الأساسي (Spreadsheet) + صلاحية كتابة داخل مجلد التخزين الخاص بالمشروع (Drive).

بهذه الصلاحية، يتحول النظام إلى:

```
الشيت الأساسي (مصدر البيانات)
        ↓  (قراءة تلقائية مجدولة)
سكربت المزامنة
        ↓  (حفظ منظم ومقسم حسب الأشهر)
مجلد تخزين بيانات المشروع
        ↓  (قراءة سريعة)
لوحة الأداء
```

---

## 4. ما هي الصلاحيات المطلوبة بالضبط؟

| الصلاحية | لماذا نحتاجها | نوع الوصول |
|---|---|---|
| قراءة الشيت الأساسي `Spreadsheets` | لقراءة أرقام الأداء تلقائياً | قراءة فقط |
| كتابة في مجلد المشروع `Drive` | لحفظ الملفات المنظمة حسب الشهر | مجلد محدد فقط |

> **مهم:** لا نحتاج صلاحيات تعديل على الشيت الأساسي إطلاقاً، نحتاج **قراءة فقط**.

---

## 5. ماذا سيتغير بعد منح الصلاحية؟

### أ) إلغاء الرفع اليدوي نهائياً
- لا حاجة للصق JSON يدوياً بعد الآن.
- البيانات تتحدث تلقائياً.

### ب) مزامنة تلقائية مجدولة
- إضافة مجدول زمني (Trigger) يعمل كل ساعة أو كل 6 ساعات أو يومياً.
- يقرأ الشيت ويحدّث اللوحة بدون تدخل بشري.

### ج) أرشفة كاملة لجميع الأشهر
- كل شهر يخزن في مجلد مستقل:
```
Agent_Performance_Hub_Data/
├── summary_overview.json      ← ملخص الشهر الحالي
├── months/
│   ├── 2026-08/
│   ├── 2026-09/
│   └── 2026-10/
```

### د) سرعة قراءة عالية (لأنها مهمة جداً)
- تُحسب الملخصات مسبقاً في وقت المزامنة.
- تُفصل ملفات الموظفين عن بعضها.
- تُستخدم الذاكرة المؤقتة (Cache) للوحة الرئيسية.
- تُسحب تفاصيل الموظف **صفحة صفحة** (50 صف فقط) بدل آلاف الصفوف دفعة واحدة.

---

## 6. لماذا هذه الصلاحية مهمة؟ (الفوائد)

1. **الدقة** — القضاء على الأخطاء البشرية في النقل اليدوي.
2. **السرعة** — تحديث البيانات بشكل شبه لحظي.
3. **الشمولية** — أرشفة تلقائية لكل شهر بدون أي جهد إضافي.
4. **الموثوقية** — لا يعتمد النظام على شخص واحد للتحديث.
5. **الوصول للمدراء** — اللوحة تعكس أرقام حقيقية محدثة في أي وقت.
6. **صفر تخريب** — الصلاحية قراءة فقط على المصدر، ولا تمس البيانات الأصلية.

---

## 7. ضمانات الأمان والحماية

- **قراءة فقط:** لن يتم تعديل أو حذف أي شيء في الشيت الأساسي.
- **صلاحية محدودة:** الكتابة مسموحة فقط داخل مجلد مخصص للمشروع.
- **عزل البيانات:** ملفات كل موظف منفصلة عن البقية.
- **سجل تدقيق:** يمكن تسجيل وقت كل مزامنة وحالة نجاحها.
- **قابلية الإلغاء:** يمكن سحب الصلاحية في أي وقت دون تأثير على البيانات الأصلية.

---

## 8. الخلاصة التنفيذية

منح صلاحية القراءة على الشيت الأساسي سيحوّل لوحة الأداء من نظام يعتمد على **الرفع اليدوي** إلى نظام **متزامن تلقائياً ومؤرشف شهرياً وسريع القراءة**، مع الحفاظ الكامل على سلامة البيانات الأصلية (قراءة فقط).

**المطلوب من صاحب الصلاحية:**
- الموافقة على مشاركة الشيت الأساسي (قراءة فقط) مع حساب السكربت.

---

> **إعداد:** مشروع لوحة أداء الموظفين
> **التصنيف:** طلب صلاحيات تقنية داخلية