# خطة ما بعد أخذ صلاحيات الوصول + المعمارية المعتمدة
## (Post-Access Plan & Approved Architecture)

> **الغرض من هذا الملف:** مرجع موحّد لكل ما تمت مناقشته واتخاذه من قرارات، حتى إذا وصلت الصلاحيات نعرف بالضبط ما الذي سننفذه، وبنفس الوقت نستطيع الرجوع للوضع الحالي في أي وقت.
>
> **مرتبط بـ:** `طلب-صلاحيات-الوصول-للبيانات.md` (وثيقة الطلب الرسمية)

---

## 1. الوضع الحالي (قبل الصلاحية) — نقطة العودة

المصدر الحالي للبيانات هو **JSON يُلصق يدوياً** عبر بوابة الإدارة (`Admin.html`):

```
أداة الجمع في صفحة العمل (Extension)
        ↓ نسخ النتائج (JSON)
لصق يدوي في Admin Portal
        ↓ حفظ ومزامنة
Code.gs → Google Drive (Agent_Performance_Hub_Data)
        ↓
لوحة الأداء تقرأ من Drive
```

**ملفات الوضع الحالي المعنية:**
- `Code.gs` — الباك إند (يستقبل JSON من `Admin.html` ويخزنه في Drive).
- `Admin.html` — بوابة لصق JSON اليدوي.
- `sync-script.js` — إرسال من المتصفح عبر Web App (بديل CORS).

> هذه الحالة تبقى كاملة وقابلة للعمل حتى بعد أخذ الصلاحية (كخطة رجوع احتياطية).

---

## 2. القرارات المعمارية المعتمدة (خلاصة النقاش)

### 2.1 هل نبقى على Google Drive؟ — نعم ✅
- **Drive** يبقى طبقة التخزين والأرشفة والكاش، لكن **دوره يتغير** من "مستودع الرفع اليدوي" إلى "طبقة ملخصات + أرشيف شهري مسبق".
- السبب: حجم الملف الواحد يصل 100MB عبر Apps Script، وعملياً لن نمتلئ (انظر القسم 6).

### 2.2 المصدر سيصبح شيت وليس JSON ⚠️
عند أخذ الصلاحية، نقرأ **صفوف الشيت الخام** (شبكة خلايا عبر `getValues()`)، وليس JSON الإضافة. لذلك نحتاج طبقة **مهايأة الشيت (Sheet Adapter)** تحوّل الخلايا إلى نفس الصيغة الداخلية الموحّدة:

```
{ agent, metric, header: [...], rows: [[...], [...]] }
```

بعد التحويل، **كل المنطق الحالي يبقى كما هو دون تغيير** (التطبيع + منع التكرار + التخزين + الكاش + الترقيم).

### 2.3 المعمارية المستهدفة (بعد الصلاحية)

```
[الشيت الأساسي]  ← مصدر الحقيقة (قراءة فقط)
        │
        ▼  Trigger تلقائي (كل ساعة/يوم)
[Apps Script = المحرك]
   ├── sheetToRecords()         ← يحوّل الخلايا لسجلات موحّدة
   ├── saveToMonths()           ← أرشفة كل شهر في Drive
   └── buildSummaries()         ← يحسب ملخصات مسبقة
        │
        ▼
[Google Drive = تخزين + كاش]
   ├── summary_overview.json    ← يُقرأ بسرعة عند فتح اللوحة
   ├── months/2026-09/...       ← أرشيف شهري
   └── drills/agent_x_csat.json ← تفاصيل موظف/مقياس
        │
        ▼
[لوحة العرض الحالية (محرك مخصص)]
   ← تقرأ الملخصات + التفاصيل المؤرشفة (سريعة)
```

---

## 3. مقارنة البدائل (للمرجعية المستقبلية)

| الخيار | ممتاز لـ | سيئ لـ | الملاءمة لحالتنا |
|---|---|---|---|
| **Apps Script + Drive + لوحة مخصصة** (الحالي) | ETL، أرشفة، لوحة مخصصة، رسوم SVG | يحتاج صيانة كود | ⭐⭐⭐⭐⭐ الأفضل |
| **AppSheet** | نماذج إدخال، CRUD سريع، موبايل | تحليلات معقدة، هيدرات مزدوجة، رسوم متقدمة | ⭐⭐ (فقط لإدخال بسيط) |
| **Looker Studio (Data Studio)** | تقارير تنفيذية جاهزة بدون كود | تحكم دقيق بالمنطق المعقد | ⭐⭐⭐⭐ مكمّل للمديرين |
| **BigQuery** | تحليلات ضخمة SQL | تكلفة + تعقيد + زمن نقل | ⭐⭐ مبالغ فيه الآن |
| **Firestore / Postgres** | متعدد مستخدمين، real-time | إعداد وصيانة أكبر | ⭐⭐ فقط لبنية ضخمة |

**الخلاصة:** نبقى على Apps Script + Drive + لوحتنا كأساس. AppSheet فقط لو أردنا نموذج إدخال يدوي للمشرفين. Looker Studio مكمّل اختياري للمديرين.

---

## 4. إيجابيات وسلبيات أخذ الصلاحيات

### ✅ الإيجابيات
| # | الإيجابية |
|---|---|
| 1 | إلغاء الرفع اليدوي نهائياً (لا لصق JSON، لا أخطاء نسخ) |
| 2 | تحديث تلقائي مجدول |
| 3 | أرشفة كل شهر تلقائياً (`months/YYYY-MM`) |
| 4 | سرعة قراءة أعلى على المدى الطويل |
| 5 | دقة واكتمال البيانات (لا صفوف مفقودة/مكررة) |
| 6 | لا اعتماد على شخص واحد للتحديث |
| 7 | قراءة فقط على المصدر (صفر خطر على البيانات الأصلية) |

### ❌ السلبيات / المخاطر وكيف نتعامل معها
| # | السلبية | الحل |
|---|---|---|
| 1 | توسيع نطاق الصلاحيات | قراءة فقط على الشيت + كتابة فقط في مجلد المشروع |
| 2 | لو تغيرت بنية الشيت تتكسر المزامنة | كشف أخطاء + سجل تدقيق + تثبيت أسماء الأعمدة |
| 3 | صلاحيات service account تحتاج موافقة أمنية | الوثيقة تشرح الضرورة + قابلة للسحب |
| 4 | تكلفة تنفيذ أولية | مرة واحدة فقط ثم كل شيء تلقائي |
| 5 | الاعتماد على حساب السكربت | حساب مشترك خاص بالمشروع |
| 6 | زمن مزامنة أولية | أول مرة تُبنى الأرشيف، بعدها sync سريع للتغييرات فقط |

---

## 5. حماية التخزين (لماذا Drive لن يمتلئ قريباً)

### الحدود الفعلية
| العنصر | الحد |
|---|---|
| سعة Google Drive الشخصي | 15 GB مجاناً |
| حجم ملف JSON واحد عبر Apps Script | حتى 100 MB |
| DriveApp يكتب لـ Drive العادي (وليس تخزين السكربت 500MB) | غير محدود عملياً بسعة الحساب |

### تقنيات الحماية المعتمدة
- **الملخصات المسبقة**: نقرأ ملخص صغير بدل التفاصيل الكاملة.
- **فصل موظف × مقياس**: الملف الواحد يبقى صغير (KB–MB).
- **التخزين الشهري**: كل شهر مجلد مستقل.
- **كتابة نص مجرد**: `MimeType.PLAIN_TEXT` (لا HTML ثقيل).
- **أرشفة الأشهر القديمة** خارج النشط عند الحاجة.

### متى ننتقل لبديل (عتبة واضحة)
ننتقل من Drive إلى **BigQuery/Firestore** فقط إذا:
- تجاوزت البيانات **5 GB+** وبدأ الأداء يتدهور.
- ظهرت حاجة لاستعلامات SQL متقدمة.
- أصبح عدد المستخدمين الكتابين المتزامنين كبيراً.

> حتى ذلك الحين: Drive + ملخصات + تقسيم شهري يكفي لسنوات.

---

## 6. خطة التنفيذ بعد وصول الصلاحية

### الخطوات المطلوبة في `Code.gs`
- [ ] **إضافة `SOURCE_SPREADSHEET_ID`** في `CONFIG` (معرف الشيت الأساسي + `SKIP_PREFIXES` للصفوف المهملة).
- [ ] **`syncFromSourceSheet()`**: تقرأ الورقات من الشيت وتحوّلها لسجلات.
- [ ] **`sheetToRecords()`**: تحويل Grid → سجلات قياسية (اكتشاف المقياس + عمود الوكيل + الهيدر المزدوج).
- [ ] **`installSyncTrigger()`**: جدولة تلقائية (كل ساعة/يوم) عبر `ScriptApp.newTrigger`.
- [ ] **`saveToMonths()`**: أرشفة شهرية `months/YYYY-MM/`.
- [ ] **إبقاء `summary_overview.json` + الكاش** كمصدر قراءة سريع للوحة.

### ما يبقى دون تغيير
- كل منطق التطبيع والحسابات (`agent-dashboard-data.js` / `Code.gs`).
- منع التكرار (Deduplication).
- واجهة العرض (`Index.html` + `Scripts.html` + `Styles.html`).
- الكاش (`CacheService`).

### ما يُضاف جديداً فقط
- طبقة `sheetToRecords()` (مهايأة الشيت).
- `syncFromSourceSheet()` + `installSyncTrigger()`.
- مجلد `months/` للأرشفة الشهرية.

---

## 7. المعلومات المطلوبة من صاحب الشيت (لأتمتة التنفيذ)

قبل أو عند وصول الصلاحية، نحتاج معرفة بنية الشيت الفعلية:

1. **أسماء الورقات (Tabs)** — هل ورقة واحدة أم ورقة لكل مقياس؟
2. **أعمدة كل ورقة** — الأسماء الدقيقة (agent, metric, ticket_id, csat, abst, agbt, break exceed, lateness…).
3. **كيف نميّز المقياس للصف** — عمود اسمه `metric` أم متريكات مفصولة بورقات/أعمدة؟
4. **كيف نميّز الموظف** — عمود `agent`/`email` أم اسم الورقة هو الموظف؟

> بدون هذه التفاصيل لا نستطيع كتابة `sheetToRecords()` بشكل صحيح، لذا تُرسل قبل بدء التنفيذ.

---

## 8. خطة الرجوع للوضع الحالي (إذا لزم)

الوضع الحالي (JSON يدوي عبر `Admin.html`) يبقى **فعالاً تماماً** بجانب المزامنة التلقائية:

- **وضع مزدوج آمن:** نُبقي مسار الـ JSON اليدوي كخيار احتياطي، والمزامنة التلقائية كمسار رئيسي.
- **إيقاف المزامنة:** نستطيع حذف الـ Trigger من `ScriptApp` بأي وقت دون المساس بالبيانات.
- **العودة الكاملة:** لو ألغيت الصلاحية، تعود العملية اليدوية كما كانت (نفس `Code.gs` و `Admin.html` الحاليين).

---

## 9. الملخص التنفيذي

> بعد أخذ الصلاحية: المصدر يصبح **شيت (قراءة فقط)**، والمحرك يحوّله لسجلات موحّدة ثم يخزنه في **Drive** بشكل **شهري + ملخصات مسبقة**، واللوحة تقرأ **ملخصات سريعة** بدل الخام. نبقى على Drive، وكل المنطق الحالي سليم، ونضيف فقط طبقة المهايأة والجدولة.

**جاهز للتنفيذ فور:**
1. وصول موافقة الصلاحية.
2. استلام بنية الشيت (الورقات + الأعمدة).
